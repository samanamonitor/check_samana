define command {
  command_name    check-samana3-load
  command_line    $USER1$/check_winrm.py -H $HOSTADDRESS$ -a $USER9$ -U $USER12$ -A "-SamanaMonitorUri $USER13$"
}

define command {
  command_name    check-samana3-load-wsgi
  command_line    $USER1$/check_winrm_wsgi.py -H $HOSTADDRESS$ -U $USER12$ -a $USER9$ -U $USER12$ -A "-SamanaMonitorUri $USER13$"
}

define command {
  command_name    check-samana3-cpu
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m cpu -w $ARG1$ -c $ARG2$
}

define command {
  command_name    check-samana3-ram
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m ram -w $ARG1$ -c $ARG2$
}

define command {
  command_name    check-samana3-swap
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m swap -w $ARG1$ -c $ARG2$
}

define command {
  command_name    check-samana3-log
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m log -s '$ARG1$' -w $ARG2$ -c $ARG3$
}

define command {
  command_name    check-samana3-services
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m services -i '$ARG3$' -e '$ARG4$' -w $ARG1$ -c $ARG2$
}

define command {
  command_name    check-samana3-hddrives
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m hddrives -w $ARG1$ -c $ARG2$
}

define command {
  command_name    check-samana3-uptime
  command_line    $USER1$/check_samana3.py -H $_HOSTID$ -U $USER13$ -m uptime -w $ARG1$ -c $ARG2$
}
